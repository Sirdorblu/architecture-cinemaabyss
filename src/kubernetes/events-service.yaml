apiVersion: apps/v1
kind: Deployment
metadata:
  name: events-service
  namespace: cinemaabyss
  labels: { app: events-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: events-service } }
  template:
    metadata: { labels: { app: events-service } }
    spec:
      containers:
        - name: events-service
          image: ghcr.io/sirdorblu/events-service:latest
          imagePullPolicy: Always
          ports: [{ containerPort: 8082 }]
          env:
            - { name: PORT, value: "8082" }
            - { name: KAFKA_BROKERS, value: "kafka:9092" }
          readinessProbe: { httpGet: { path: /api/events/health, port: 8082 }, initialDelaySeconds: 5, periodSeconds: 10 }
      imagePullSecrets:
        - name: ghcr-creds
---
apiVersion: v1
kind: Service
metadata:
  name: events-service
  namespace: cinemaabyss
spec:
  selector: { app: events-service }
  type: NodePort
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      nodePort: 30082

